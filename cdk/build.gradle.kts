// Placeholder — real configuration lands in Phase C (CDK v2 Java infrastructure).
// This empty script exists so Gradle can evaluate `include("cdk")` in settings.gradle.kts
// and generate the wrapper without error.
