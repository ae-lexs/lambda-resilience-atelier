// Placeholder — real configuration lands in Phase B (Spring Boot Lambda).
// This empty script exists so Gradle can evaluate `include("api")` in settings.gradle.kts
// and generate the wrapper without error.
